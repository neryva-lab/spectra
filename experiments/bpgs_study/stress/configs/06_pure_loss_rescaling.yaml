name: 06_pure_loss_rescaling
group: stress
kind: empirical
description: Pure post-hoc loss-rescaling stress with unchanged underlying targets.
notes: Primary scale-robustness evidence for the paper.
variants:
  - label: x1
    experiment: exp_07_pure_loss_rescaling
    family: rescaling
    methods: [bpgs, uwso, kendall]
    seeds: [42, 43, 44]
    overrides:
      - experiment.training_stress.loss_scale_multiplier=1.0
  - label: x10
    experiment: exp_07_pure_loss_rescaling
    family: rescaling
    methods: [bpgs, uwso, kendall]
    seeds: [42, 43, 44]
    overrides:
      - experiment.training_stress.loss_scale_multiplier=10.0
  - label: x100
    experiment: exp_07_pure_loss_rescaling
    family: rescaling
    methods: [bpgs, uwso, kendall]
    seeds: [42, 43, 44]
    overrides:
      - experiment.training_stress.loss_scale_multiplier=100.0
  - label: x1000
    experiment: exp_07_pure_loss_rescaling
    family: rescaling
    methods: [bpgs, uwso, kendall]
    seeds: [42, 43, 44]
    overrides:
      - experiment.training_stress.loss_scale_multiplier=1000.0
