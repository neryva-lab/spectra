# @package _global_
method_name: bpgs

method:
  name: bpgs
  s_min: -10.0
  s_max: 10.0
  s_init: 0.0
  s_mode: batch_aware
  init_mode: auto_calibrate
  theta_grad_scale: 100.0
  split_stop_gradient: true
  temperature: 2.0
  kappa: 0.1

module:
  engine:
    _target_: spectra.engine.optimizers.BPGSEngine

train:
  grad_clip: 10.0
